
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-2974: Check that Feedback Form is translated to Spanish", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2971: Check all fields in feedback form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2973: Check that user can't send empty Feedback Form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2152: Check that Feedback Form not sent when not all required fields filled in", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2952: Check that user can close Feedback Form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2953: Check that data saved when user closed Feedback Form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2954: Check that user can Clear Feedback Form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2955: Check that user can close Clear Feedback Form popup", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2968: Check that user can remove attached picture from Feedback form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2957: Check that user can upload image by making a photo to Feedback form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2975: Check Save and finish later button", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2967: Check that user can send Feedback with all required fields filled in", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2978: Check that user can't attach more than 5 files to Feedback form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2979: Check that user can't send Feedback Form in Offline mode", async function(app: NightwatchAPI) {
        app.end();
    });

});
