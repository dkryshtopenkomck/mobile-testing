
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-6860: Check job and its work team block (Team Lead)", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-6834: User can remove worker from existing job", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-6817: User can add new worker for existing job", async function(app: NightwatchAPI) {
        app.end();
    });


    it("CXF-15468: Update job status - Scheduled", async function(app: NightwatchAPI) {
        app.end();
    });


    it("CXF-15469: Update job status - Unassigned", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-15470: Update job status - In Progress", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-15471: Update job status - Ready for Approval", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-15472: Update job status - Completed", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-15473: Update job status - Cancelled", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-15600: Insufficient rights - update job status with non-team lead user role", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-10145: User can check in and check out job with face id", async function(app: NightwatchAPI) {
        app.end();
    });


    it("CXF-10144: Crew type Skill without Certificates on the Crew & Certificates tab", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-10150: Check in & Check out tab disabled if crew type is not defined", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-6952: User can see the history items from Check In/Out tab", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-7145: User opens RA sessions tab and sessions list is empty", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-7144: User opens RA sessions tab and sessions are displayed", async function(app: NightwatchAPI) {
        app.end();
    });



});
