
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-6499: User can open Azimuth Check", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-6561: Check Azimuth Check Spanish translations", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-13663: Check that user can successfully save a report", async function(app: NightwatchAPI) {
        app.end();
    });

});
