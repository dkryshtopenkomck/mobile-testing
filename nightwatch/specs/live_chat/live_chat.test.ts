
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-9775: User opens chat activity and chats are displayed", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-9821: User can open conversation from chat button (worker)", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-15187: User can see members activity from the chat", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-12787: Adding user to the chat", async function(app: NightwatchAPI) {
        app.end();
    });


    it("CXF-10209: User can check conversation history", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-10802: Check user can create a private conversation with ASP Members", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-10920: Check user can create a Group chat", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-11756: User can upload a photo in a private chat", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-11759: User can upload a photo in a group chat", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-11967: User can text and upload a photo in a private chat", async function(app: NightwatchAPI) {
        app.end();
    });


    it("CXF-11968: User can text and upload a photo in a group chat", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-9949: User can search existing chat by name", async function(app: NightwatchAPI) {
        app.end();
    });


});
