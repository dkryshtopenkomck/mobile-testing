
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-7084: Clicking on the cancel button closes Face ID dialog", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-7083: If no face detected the error appears", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-7082: Successfully configure Face IDs", async function(app: NightwatchAPI) {
        app.end();
    });

});
