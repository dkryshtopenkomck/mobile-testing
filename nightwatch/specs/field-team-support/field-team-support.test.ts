
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-5358: Check spanish translation for ticketing system form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-5632: Ticket Details - Add Comment without attachment", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-5019: Test ticket details screen", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-5059: Search option (Existing ticket)", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-7076: Search option (Invalid, ticket does not exist)", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-7077: User can not submit empty form, cancel button navigates back", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-5023: Create new ticket", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-5649: Ticket Details - Add Comment with attachment", async function(app: NightwatchAPI) {
        app.end();
    });


});
