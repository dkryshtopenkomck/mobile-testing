
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-433: If checkbox is checked, the wizard does not reappear on relaunch", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2316: Check correct translation to Spanish on Installation Wizard", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2009: User clicks on Cancel button and wizard becomes closed", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2025: If checkbox is unchecked, the wizard reappears on relaunch", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2236: Test skipping of installation wizard window", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2056: Check that user can skip installation procedure of the app", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2042: Check that user can cancel installation procedure of the app", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2031: Check downloading of several apps from installation wizard", async function(app: NightwatchAPI) {
        app.end();
    });


    it("CXF-2057: Check downloading of several apps from installation wizard", async function(app: NightwatchAPI) {
        app.end();
    });


    it.skip("CXF-2004: Check downloading of PIA from installation wizard", async function(app: NightwatchAPI) {
        app.end();
    });
    it.skip("CXF-2005: Check downloading of ESI from installation wizard", async function(app: NightwatchAPI) {
        app.end();
    });
    it.skip("CXF-2007: Check downloading of SDEM from installation wizard", async function(app: NightwatchAPI) {
        app.end();
    });
    it.skip("CXF-2011: Check downloading of SA from installation wizard", async function(app: NightwatchAPI) {
        app.end();
    });
    it.skip("CXF-2026: Check downloading of RA from installation wizard", async function(app: NightwatchAPI) {
        app.end();
    });
    it.skip("CXF-2028: Check downloading of Gensuite from installation wizard", async function(app: NightwatchAPI) {
        app.end();
    });
    it.skip("CXF-2033: Check downloading of SRS from installation wizard", async function(app: NightwatchAPI) {
        app.end();
    });


});
