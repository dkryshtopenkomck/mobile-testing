
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-2440: Check that user can tap back arrow", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2435: Test scanning of invalid barcode", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2439: Scan wiped barcode", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2148: Test scanning of valid barcode without text", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2441: Check that user can tap back arrow after the barcode is scanned", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2437: Test scanning of barcode with bad quality", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2438: Test scanning of valid barcode with text", async function(app: NightwatchAPI) {
        app.end();
    });

});
