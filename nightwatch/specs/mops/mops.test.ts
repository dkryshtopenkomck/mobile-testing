
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-3257: Check that List of MOPs screen is correctly translated to Spanish", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-3258: Check that Installation MOP screen is correctly translated to Spanish", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-3251: Check that user can open MOP screen", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-3249: Check that user can open list of MOPs", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-3250: Check that user can close list of MOPs", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-3252: Check that user can close Installation MOP screen", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-3254: Check that user can close activity from Installation MOP screen", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-3253: Check that user can open activity from Installation MOP screen", async function(app: NightwatchAPI) {
        app.end();
    });


});
