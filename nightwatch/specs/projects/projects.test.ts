
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-1449: Check the filtering for the work plans list page with Date field", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1452: Check the popup with pin details appears", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1453: Check Refresh button in offline mode", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1486: Check the filtering for the work plans list page with Due Date field", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1487: Return back from the WP list", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1488: Unselect pin on the map", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1489: Open the empty WP list", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1490: Check the navigation map link", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1491: Check multi selection filter", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2318: Check correct translation to Spanish of map view in Work Plans tile", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2326: Check correct translation to Spanish of Filters Page in Work Plans tile", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2335: Check correct translation of Work Plans details to Spanish", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2336: Check correct translation of map view to Spanish in offline mode", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-8411: Weather on Site forecast page", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-8496: Spanish translation for Weather on Site", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-8913: User can change project source between Erisite and Sitetracker", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1445: Check 'Work Plans' Screen is displayed correctly", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1446: Check the list of all Test Plans", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1492: Check Search function", async function(app: NightwatchAPI) {
        app.end();
    });

});
