
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-2317: Check correct translation to Spanish of Help and Launch buttons", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2032: Check launching 'CMS' application", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2034: Check launching 'EriSite Americas' application", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2035: Check launching 'EriSite APAC' application", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2036: Check launching 'EriSite EMEA' application", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2037: Check launching 'ESR' application", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-12684: Launch button becomes disabled if no internet connection", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-12685: When user goes online, launch button becomes enabled again", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-11759: User can upload a photo in a group chat", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-11967: User can text and upload a photo in a private chat", async function(app: NightwatchAPI) {
        app.end();
    });


    it("CXF-11968: User can text and upload a photo in a group chat", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-9949: User can search existing chat by name", async function(app: NightwatchAPI) {
        app.end();
    });


});
