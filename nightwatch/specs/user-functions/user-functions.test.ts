// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-1378: Check correct translation of User Functions to Spanish in sidebar", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2315: Check correct translation of User Functions to Spanish on Main Menu", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1375: Check list of all user functions in side menu", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1376: Check list of all functions and sub-functions main screen", async function(app: NightwatchAPI) {
        app.end();
    });

});
