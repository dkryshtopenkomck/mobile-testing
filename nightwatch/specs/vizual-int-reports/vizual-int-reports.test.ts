// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('Visual int report', function() {

    it("CXF-669: If checkbox is skipped, the tips modal reappears on relaunch", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-3891: Check Welcome Screen", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-3892: Visual Intelligence Reports is translated in Spanish correctly", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-6511: If checkbox is checked, the tips modal does not reappear on relaunch", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-6720: Create report successfully", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-6721: Clicking on discard button it reset form state", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-6722: Clicking on start over button it redirect back to the VI form", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-13664: When user clicks on discard button it reset report form", async function(app: NightwatchAPI) {
        app.end();
    });

});
