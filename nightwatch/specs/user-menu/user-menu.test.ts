// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-2310: Check correct translation to Spanish in User Menu", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2312: Check correct translation to Spanish on About Page in user menu", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-2313: Spanish translation for the Report issue page", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-17662: User can hide new features banner at the very bottom", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-17663: User cancels confirmation modal and new features banner is visible", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-436: User can report an issue", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1353: Check User Menu screen", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-7367: Configure Face ID option is absent for user with particular role", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-1367: Check User Menu links and functions", async function(app: NightwatchAPI) {
        app.end();
    });

});
