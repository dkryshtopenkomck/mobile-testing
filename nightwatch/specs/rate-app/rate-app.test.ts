
// Imports //

import {NightwatchAPI} from 'nightwatch';

// Tests //

describe('User menu', function() {

    it("CXF-16688: Successful form submission with a star rating and comment", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-16689: Successful form submission with only a star rating", async function(app: NightwatchAPI) {
        app.end();
    });

    it("CXF-16690: Cancel button clears the form", async function(app: NightwatchAPI) {
        app.end();
    });

});
